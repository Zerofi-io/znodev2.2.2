export const P2P_ROUNDS = {
  MINT_SIGNATURE: Number(process.env.MINT_SIGNATURE_ROUND || 9800),
  MULTISIG_SYNC: (() => {
    const mint = Number(process.env.MINT_SIGNATURE_ROUND || 9800);
    let sync = Number(process.env.MULTISIG_SYNC_ROUND || (mint + 10));
    if (sync === mint) sync = mint + 10;
    return sync;
  })(),
  WITHDRAWAL_UNSIGNED_INFO: Number(process.env.WITHDRAWAL_UNSIGNED_INFO_ROUND || 9898),
  WITHDRAWAL_SIGNED_INFO: Number(process.env.WITHDRAWAL_SIGNED_INFO_ROUND || 9902),
  WITHDRAWAL_LEGACY_SIGNATURE: 9901,
};
export const P2P_MESSAGE_LIMITS = {
  MAX_MESSAGE_SIZE: 2 * 1024 * 1024,
  MAX_BUFFER_SIZE: 10 * 1024 * 1024,
  MAX_REPLAY_CACHE_SIZE: 5000,
  MESSAGE_TTL_MS: 180000,
};
export const P2P_RATE_LIMITS = {
  PER_PEER_MESSAGES_PER_SECOND: 100,
  BURST_SIZE: 200,
};
export const P2P_TIMEOUTS = {
  ROUND_COMPLETION_MS: 300000,
  RPC_CALL_MS: 300000,
  STREAM_WRITE_MS: 10000,
  STREAM_READ_MS: 5000,
};
export const P2P_MEMORY_LIMITS = {
  ROUND_DATA_TTL_MS: 4 * 60 * 60 * 1000,
  ROUND_DATA_MAX_ENTRIES: 10000,
  PENDING_CLUSTERS_MAX: 50,
  CLEANUP_INTERVAL_MS: 15 * 60 * 1000,
  SIGNATURE_CACHE_MAX: 1000,
  CONSENSUS_STATE_TTL_MS: 30 * 60 * 1000,
};
export const BRIDGE_LIMITS = {
  PENDING_DEPOSITS_MAX: 1000,
  PENDING_WITHDRAWALS_MAX: 5000,
  PROCESSED_HISTORY_MAX: 10000,
  IDEMPOTENCY_TTL_MS: 24 * 60 * 60 * 1000,
};
