export const HEALTH_DEFAULTS = {
  SWEEP_COORDINATOR_TIMEOUT_MS: 5000,
  SWEEP_GRACE_PERIOD_MS: 3600000,
  SWEEP_ATTEMPT_COOLDOWN_MS: 1800000,
  SWEEP_LOCK_TIMEOUT_MS: 600000,
  SWEEP_INITIATOR_EPOCH_MS: 3600000,
  MIN_SWEEP_BALANCE_ATOMIC: 100000000000n,
  CLUSTER_THRESHOLD: 7,
  REQUIRED_STAKE_ZFI: 1000000n,
  ZFI_DECIMALS: 18,
  DISSOLUTION_WATERFALL_TIMEOUT_MS: 120000,
  DISSOLUTION_FALLBACK_GAS_LIMIT: 200000n,
  BACKOFF_BASE_MS: 300000,
  BACKOFF_MAX_MS: 3600000,
  BACKOFF_MAX_FAILURES: 5,
};
export const SWEEP_STATES = {
  IDLE: 'idle',
  CHECKING: 'checking',
  SWEEPING: 'sweeping',
  DISSOLVING: 'dissolving',
  COMPLETED: 'completed',
  FAILED: 'failed',
};
export const BLACKLIST_THRESHOLD = 3;
